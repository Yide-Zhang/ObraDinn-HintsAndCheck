使用说明：

1. 确保已安装 Python。
2. 安装依赖库 Pillow（用于图像处理）：
   打开终端或命令行，运行：
   pip install -r requirements.txt
   
   或者直接运行：
   pip install Pillow

3. 运行程序：
   python run_viewer.py

程序功能：
- 自动读取 FacesHi 下的图片。
- 每页显示 10 张（2行 x 5列）。
- 点击图片可查看详情大图。
- 只有 60 张图片，共 6 页。
